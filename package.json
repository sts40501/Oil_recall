{
  "name": "oil-recall-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Taiwan E-Invoice API to query carrier history and prevent CORS issues.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
